{
  "name": "linkedin-api-caller",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "random-js": "^2.1.0",
    "socket.io": "^4.7.2"
  },
  "description": "API caller CLI with web UI for LinkedIn profile scraping",
  "scripts": {
    "start": "NODE_ENV=prod bun server.js",
    "start:dev": "concurrently -n mock-api,scrapper \"bun run mock.js\" \"bun --watch server.js\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
